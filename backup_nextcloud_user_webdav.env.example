# Copy this file to `backup_nextcloud_user_webdav.env` and fill in values.

# Nextcloud URL (e.g., https://your-nextcloud.local)
NC_URL=

# Path to CSV with lines in the format: username,app_password
# Can be absolute or relative to the script directory
USERS_FILE=./backup_nextcloud_user_webdav.csv

# USB mount point and base backup directory
USB_MOUNT_POINT=/media/USER/YOUR_DISK
BACKUP_BASE=/media/USER/YOUR_DISK/nextcloud-users

# Backup mode: 'snapshot' (full copy per run) or 'incremental'
# - snapshot: copies all files into a new timestamped folder per run
# - incremental: syncs to a fixed folder and moves changed/removed files to a versioned backup dir
MODE=snapshot

# General options
# How many snapshots to keep per user
RETENTION=1
# Compress snapshots (true/false)
COMPRESS=false
# Generate .sha256 for tar.gz (true/false)
GENERATE_SHA256=true
# Skip TLS verification for rclone WebDAV (true/false)
TLS_SKIP_VERIFY=true

# Exclusions (rclone patterns). You can use a multi-line string.
# Example:
# EXCLUDES='*/Temp/**
# */node_modules/**'
EXCLUDES=

# Binaries (adjust paths if needed)
RCLONE_BIN=rclone
CURL_BIN=curl

# CallMeBot (optional). Leave empty to disable notifications
CALLMEBOT_PHONE=
CALLMEBOT_APIKEY=
CALLMEBOT_ADMIN_NAME=
